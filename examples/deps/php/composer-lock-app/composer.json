{
  "name": "codefence/example-composer-lock-app",
  "description": "Fixture: ranged composer.json resolved by composer.lock (fake project, not installable).",
  "private": true,
  "require": {
    "php": ">=8.1",
    "symfony/http-foundation": "^5.0",
    "guzzlehttp/guzzle": "^6.5"
  },
  "require-dev": {
    "phpunit/phpunit": "^9.5"
  }
}
